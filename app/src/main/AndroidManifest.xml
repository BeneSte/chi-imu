<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.perceptualui.imustudy">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SENSOR_TYPE_ACCELEROMETER" />
    <uses-permission android:name="android.permission.SENSOR_TYPE_GYROSCOPE" />
    <application android:name="org.perceptualui.imustudy.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="org.perceptualui.imustudy.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="org.perceptualui.imustudy.TOUCH_BROADCAST"/>
                <action android:name="org.perceptualui.imustudy.KEYBOARD_BROADCAST"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
